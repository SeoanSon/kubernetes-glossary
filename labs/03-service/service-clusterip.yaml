apiVersion: v1
kind: Service
metadata:
  name: web-server-clusterip
  namespace: default
  labels:
    app: web-server
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: web-server
    tier: backend
